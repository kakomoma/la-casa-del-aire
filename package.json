{
  "name": "la-casa-del-aire",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    
    "optimize:images": "node scripts/optimize-images.mjs",
    "generate:favicon": "node scripts/generate-favicon.mjs",

    "prepare:assets": "npm run clean:icons && npm run optimize:images && npm run generate:favicon"
  },
  "devDependencies": {
    "fast-glob": "^3.3.3",
    "sharp": "^0.34.4",
    "svgo": "^4.0.0",
    "vite": "^5.0.0"
  }
}
